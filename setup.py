from setuptools import find_packages, setup

setup(
    name='siamese_network_generator',
    packages=find_packages(),
    version='0.1.0',
    description='A generator of a siamese networks for images',
    author='Dragos Moldovan',
    license='',
    install_requires=[],
)
